services:
  - type: web
    name: hallucination-audit-pipeline
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /actuator/health
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: JAVA_TOOL_OPTIONS
        value: "-Xmx384m"
